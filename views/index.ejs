<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Food List</title>
</head>
<body>
  <h1>Food List</h1>
  
  <ul>
    <% foods.forEach(food => { %>
      <li>
        <strong><%= food.name %></strong> - <%= food.description %> - <%= food.calories %> calories
        <% if (food.isVegetarian) { %>
          (Vegetarian)
        <% } %>
        <img src="<%= food.image %>" alt="<%= food.name %>" width="100"><br>
        <a href="/food/<%= food._id %>/edit">Edit</a>
        <form action="/food/<%= food._id %>?_method=DELETE" method="POST" style="display:inline;">
            <button type="submit">Delete</button>
          </form>
      </li>
    <% }) %>
  </ul>

  <a href="/food/new">Add New Food</a>
</body>
</html>